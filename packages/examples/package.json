{
  "name": "examples",
  "version": "0.0.0-dev",
  "private": true,
  "scripts": {
    "lint": "eslint . --ext .ts",
    "build": "rm -rf lib && tsc --declaration"
  },
  "devDependencies": {
    "dotenv": "16.4.5",
    "eslint-config-shared": "*",
    "ring-client-api": "13.2.0",
    "tsconfig": "*"
  }
}
